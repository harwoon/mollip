server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # react-router-dom(SPA) 라우팅: /some/path로 새로고침해도 index.html로 fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
